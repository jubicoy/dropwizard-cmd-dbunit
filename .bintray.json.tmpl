{
  "package": {
    "name": "dropwizard-cmd-dbunit",
    "repo": "maven",
    "subject": "jubicoy"
  },

  "version": {
    "name": "${TRAVIS_TAG}",
    "desc": "Tagged build",
    "released": "${RELEASED_DATE}",
    "vcs_tag": "${TRAVIS_TAG}",
    "attributes": [],
    "gpgSign": false
  },

  "files": [
    {
      "includePattern": "target/(dropwizard-cmd-dbunit-${TRAVIS_TAG}(.*)\.jar)",
      "uploadPattern": "/fi/jubic/dropwizard-cmd-dbunit/${TRAVIS_TAG}/$1"
    },
    {
      "includePattern": "./(pom.xml)",
      "uploadPattern": "/fi/jubic/dropwizard-cmd-dbunit/${TRAVIS_TAG}/dropwizard-cmd-dbunit-${TRAVIS_TAG}.pom"
    }
  ],
  "publish": true
}
